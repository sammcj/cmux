{
  "name": "@cmux/host-screenshot-collector",
  "version": "0.1.0",
  "description": "Screenshot collector script for cmux sandbox environments",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --minify",
    "typecheck": "bunx tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.54",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.9.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "files": [
    "dist"
  ]
}
